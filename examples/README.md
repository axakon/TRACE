# Examples

Projects where the TRACE framework is applied. Each example shows the structure in real use — `AGENTS.md`, `docs/architecture/overview.md`, `docs/adr/` — populated for a concrete codebase.

The first example is **TRACE itself**. The repo's own root-level structure (this very project) demonstrates the minimum dogfooded layout. See `../docs/architecture/overview.md` for what each piece does.

Additional examples are added as the playbook is applied to real customer and internal projects. Each example links back to which playbook phases shaped it.

## Index

- **TRACE itself** — minimum dogfooded structure. See repo root.

_(more to come)_
